<%= form_for(@shop_director) do |f| %>
  <% if @shop_director.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop_director.errors.count, "error") %> prohibited this shop_director from being saved:</h2>

      <ul>
      <% @shop_director.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
