<h1>Listing shop_directors</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shop_directors.each do |shop_director| %>
  <tr>
    <td><%= link_to 'Show', shop_director %></td>
    <td><%= link_to 'Edit', edit_shop_director_path(shop_director) %></td>
    <td><%= link_to 'Destroy', shop_director, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shop director', new_shop_director_path %>
